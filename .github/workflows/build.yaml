name: "Build Process"

on:
  push:
    branches:
    paths:
      - 'EUC.Profile.Buddy.Web/**' 
      - 'EUC.Profile.Buddy.Web.Test/**' 
  pull_request:
    branches: ["main"]
    paths:
      - 'EUC.Profile.Buddy.Web/**'
      - 'EUC.Profile.Buddy.Web.Test/**' 

jobs:
  build:
    name: "Build & Test"

    permissions: write-all

    runs-on: windows-latest

    steps:
      - name: Check Out Code Base
        uses: actions/checkout@v4
        with:
          fetch-depth: 0        
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            8.x
      - name : Coverlet Testing
        uses: b3b00/coverlet-action@1.2.5
        with:
          testProject: 'EUC.Profile.Buddy.Web.Test/EUC.Profile.Buddy.Web.Tests.csproj'
          output: 'lcov.info'
          outputFormat: 'lcov'
          excludes: '[program]*,[test]test.*'
      - name: Coveralls Reporting
        uses: coverallsapp/github-action@v2
